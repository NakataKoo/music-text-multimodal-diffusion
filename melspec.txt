# メルスペクトルグラムの取得========================================================
n_fft = 1024
win_length = None
hop_length = 512
n_mels = 128

mel_spectrogram = MelSpectrogram(
    sample_rate= 48000,
    n_fft=n_fft,
    win_length=win_length,
    hop_length=hop_length,
    center=True,
    pad_mode="reflect",
    power=2.0,
    norm='slaney',
    onesided=True,
    n_mels=n_mels,
)